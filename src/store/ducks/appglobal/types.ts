export {};

// import { GoogleLoginProps } from 'react-google-login';
// import { generateTypes } from '~/services/utils';

// const Types = (name: string): Object => ({
//   REQUEST: `${name}/REQUEST`,
//   SUCCESS: `${name}/SUCCESS`,
//   FAILURE: `${name}/FAILURE`,
// });

// export default generateTypes('Login');

// export enum LoginTypes {
//   REQUEST = 'request',
//   SUCCESS = 'success',
//   FAILURE = 'failure',
// }

// export interface Login {
//   id: number;
//   name: string;
// }

// export interface FormData {
//   nome: string;
//   pass: string;
// }

// export interface LoginState {
//   readonly data: GoogleLoginProps | null;
//   readonly form: FormData | null;
//   readonly loading: boolean;
//   readonly error: string | null;
// }
